# Local database notes

MongoDB Community Edition runs locally at `mongodb://127.0.0.1:27017/iplanet_portal`.

The seed script lives at `backend/src/seed.js` and creates shared corporate, service, device-master, device, ticket, engineer, and timeline data.
